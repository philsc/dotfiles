[redshift]
temp-day=6500
temp-night=3500
; Brightness is deliberately not set here: it defaults to 1.0 (untouched).
; The backlight is driven by the awesome key bindings instead.
location-provider=geoclue2
adjustment-method=randr
