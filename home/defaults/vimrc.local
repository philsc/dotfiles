" Edit this path to match your local libclang.so.1 if not
" set in /etc/vim/vimrc.local
"let g:clang_library_path='/usr/lib/llvm-3.6/lib/libclang.so.1'

" Disable clang-complete
let g:clang_complete_loaded=1
