module(
    name = "dotfiles",
    version = "0.0.0",
)

bazel_dep(name = "rules_pkg", version = "1.3.0")

# Lua interpreter for unit testing the awesome config, built from source.
# 5.3.6 matches what awesome is compiled against.
bazel_dep(name = "lua", version = "5.3.6")
